PerfumeSearch:
  post:
    tags:
      - perfume
    operationId: searchPerfume
    summary: Поиск записей о парфюме
    description: Метод поиска записей о парфюме
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: './schemas/perfume.yaml#/SearchEmergencyStubsRequest'
    responses:
      "200":
        description: Успешный ответ
        content:
          application/json:
            schema:
              $ref: './schemas/perfume.yaml#/SearchPerfumeResponse'
      "400":
        $ref: '../index.yaml#/components/responses/BadRequest'
      "500":
        $ref: '../index.yaml#/components/responses/ServerError'

PathId:
  in: path
  name: id
  required: true
  schema:
    type: integer
    minimum: 1
  description: Числовой id
  example: 1
